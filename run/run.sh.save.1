#!/bin/sh

NAME=statov
SRCDIR=statov

GUNICORN=/usr/bin/gunicorn_django
HOMEDIR=~/web/

VIRTUALENV=${HOMEDIR}/env
PROJECTDIR=${HOMEDIR}/${NAME}
SOCKFILE=/tmp/${NAME}.sock
MANAGE=${PROJECTDIR}/manage.py

. ${VIRTUALENV}/bin/activate
python ${MANAGE} run_gthon ${MANAGE} run_gunicorn --bind unix:${SOCKFILE} unicorn --bind unix:${SOCKFILE} 
